import React from 'react';
import { StyledWrapper } from './StyledEditContainer';

const EditContainer = (props) => {
  return <StyledWrapper>EditContainer</StyledWrapper>;
};

export default EditContainer;
